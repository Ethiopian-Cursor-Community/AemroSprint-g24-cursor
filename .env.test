# Optional overrides for Playwright (loaded after .env.local).
# AUTH_SECRET is auto-set by tests/playwright-env.ts if still missing.
# POSTGRES_URL is required for guest login during e2e tests — copy from .env.local.
